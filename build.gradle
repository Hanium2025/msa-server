plugins {
    id 'java'
}

allprojects {
    group = 'hanium'
    version = '0.0.1-SNAPSHOT'

    repositories {
        mavenCentral()
    }
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}